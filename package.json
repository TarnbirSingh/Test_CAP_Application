{
    "name": "test-cap-application",
    "version": "1.0.0",
    "description": "A simple SAP CAP application based on bookshop example",
    "main": "index.js",
    "scripts": {
        "start": "cds run",
        "watch": "cds watch",
        "build": "cds build --production",
        "deploy": "cds deploy"
    },
    "dependencies": {
        "@sap/cds": "^7",
        "@sap/hdi-deploy": "^4",
        "express": "^4"
    },
    "devDependencies": {
        "@sap/cds-dk": "^7"
    },
    "engines": {
        "node": ">=18"
    },
    "cds": {
        "requires": {
            "db": {
                "kind": "hana-cloud"
            }
        }
    }
}
